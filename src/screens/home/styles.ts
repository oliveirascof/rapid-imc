import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    margin: 0,
    padding: 0,
  },

  button: {
    width: 150,
    height: 50,
    borderRadius: 10,
    backgroundColor: '#abc344',
    justifyContent: 'center',
    alignItems: 'center',
  },

  textButton: {
    color: '#fff',
    fontSize: 20,
  }
})

export default styles
